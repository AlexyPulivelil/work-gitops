replicaCount: 1

image:
  repository: nginx
  tag: "1.27"

service:
  type: ClusterIP

resources:
  requests:
    cpu: 50m
    memory: 64Mi
  limits:
    cpu: 100m
    memory: 128Mi

ingress:
  enabled: false

autoscaling:
  enabled: false
